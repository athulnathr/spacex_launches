const initalState = {
  listLoadig: true,
};

const ListingReducer = (state = initalState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default ListingReducer;
